import * as React from 'react';

import StoryList from './components/StoryList';

const App = () => {
    return (
        <div className={'container'}>
            <StoryList />
        </div>
    );
};

export default App;
